#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define Boost                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL)
int main()
{
    Boost;
    pair<int, int> p;
    pair<int, int> q;

    cin >> p.first;
    cin >> p.second;

    cout << p.first;
    cout << p.second;

    int a, b;
    cin >> a >> b;
    make_pair(a, b);

    cout << q.first;
    cout << q.second;

    return 0;
}