#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define Boost                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL)
int main()
{
    Boost;
    pair<int, int> p;

    cin >> p.first >> p.second;

    cout << p.first << " " << p.second;

    return 0;
}